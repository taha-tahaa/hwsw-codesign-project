# virtualenv
.venv/

# python
__pycache__/
*.pyc

# perf / benchmark artifacts (regenerated by the scripts)
results/*.data
results/*.data.old
results/*.ppm
perf.data*

# FlameGraph is cloned by script_*.sh setup
tools/FlameGraph/

# presentation: only the .pptx is tracked. The speaking script, timing plan and
# the deck generator stay local.
presentation/*
!presentation/*.pptx
