================================================================================
  report_raytrace.txt
  HW/SW Co-design (00460882) - Final Project
  Benchmark 2 of 2: raytrace  (pure-Python recursive ray tracer)
================================================================================

  WHERE THESE NUMBERS COME FROM - INSIDE THE COURSE QEMU IMAGE
  ------------------------------------------------------------
  Every figure in this report was measured INSIDE the provided QEMU guest, as
  the course requires.  Nothing here was measured on the naranja host.

    virtualization  systemd-detect-virt = kvm        <- confirms the VM
    distro          Ubuntu 22.04.5 LTS
    kernel          5.15.0-1080-kvm
    vcpus / memory  8 / 3938 MB
    host cpu        Intel Xeon E5-2630 v3 @ 2.40GHz (guest PMU: haswell)
    python          3.10.12      pyperf 2.10.0      perf 5.15.179
    iverilog        11.0         py-spy 0.4.2
    pinning         taskset -c 2

  Launched from /scratch/ece882-031 with -cpu host,pmu=on over a qcow2 overlay,
  so the course image is never written to.  The pmu=on flag is what makes
  hardware counters available in the guest at all - see report_pyflate.txt
  section 2 for the full list of what perf can and cannot do here.

  Full record: results_qemu/environment.txt.  Reproduce: ./script_raytrace.sh


  REQUIREMENT CHECK - >= 7% ON TWO OR MORE BENCHMARKS
  ---------------------------------------------------
  Project.pdf, instruction 6: "If you can identify two or more benchmarks where
  your optimizations result in a performance improvement of at least 7%, that
  will be considered sufficient."

  +------------+------------------+-----------------+-----------+---------+---------------------+
  | Benchmark  | Baseline         | Optimized       | Runtime   | Speedup | Significance        |
  |            |                  |                 | cut       |         |                     |
  +============+==================+=================+===========+=========+=====================+
  | pyflate    | 1115.1 ms +- 10  | 444.6 ms +- 6   |  60.1%    |  2.51x  | Significant t=676.00|
  +------------+------------------+-----------------+-----------+---------+---------------------+
  | raytrace   |  809.3 ms +- 12  | 282.2 ms +- 14  |  65.1%    |  2.87x  | Significant t=315.11|
  +------------+------------------+-----------------+-----------+---------+---------------------+

  "7% improvement" is ambiguous between two readings; both are satisfied:
      as a RUNTIME CUT   60.1% and 65.1%   (7% required)
      as a SPEEDUP       2.51x and 2.87x, i.e. 151% and 187% faster (1.07x needed)

  Output is unchanged: pyflate byte-identical over all 399,360 decompressed
  bytes, raytrace bit-identical over the whole 30,000-byte framebuffer and at
  seven different resolutions.


================================================================================
1. OVERVIEW
================================================================================

WHAT THE BENCHMARK DOES
-----------------------
raytrace is the toy ray tracer by Callum and Tony Garnock-Jones (2008).  It
renders a 100x100 image of a scene containing:

    1 large sphere        centre (1, 3, -10),  radius 2
    6 small spheres       centres (-3-0.4y, 2.3, -5), radius 0.4
    1 checkerboard plane  (Halfspace through the origin, normal +Y)
    2 point lights        (30, 30, 10) and (-10, 100, 30)

for a total of 8 intersectable objects.  Shading is Lambert diffuse plus a
specular reflection, recursing to depth 3.  Output is a PPM framebuffer.

Choosing this alongside pyflate was deliberate.  pyflate is integer, control-
flow and irregular-memory bound; raytrace is floating-point and allocation
bound.  They stress opposite parts of the machine and, as section 5 shows, they
motivate the two DIFFERENT accelerator paradigms from the lecture.

LIBRARIES USED
--------------
Only math (sqrt, tan, pi) and array (the framebuffer).  As with pyflate, the
point is to measure the interpreter doing real arithmetic, not a C library.

DATA STRUCTURES AND ALGORITHM
-----------------------------
  Vector / Point      three floats, distinguished only by behaviour.  Point -
                      Point yields a Vector; Point + Vector yields a Point.
                      The distinction is enforced at RUNTIME by isPoint() /
                      mustBeVector() calls inside every arithmetic operator.
  Sphere / Halfspace  geometry, each exposing intersectionTime() and normalAt()
  Ray                 origin Point + normalized direction Vector
  Scene               object list, light list, camera, recursion depth
  Canvas              array('B') RGB framebuffer

Per pixel: build a primary ray, test it against all 8 objects, take the nearest
hit, shade it (which casts shadow rays to each light and one reflection ray),
recurse.

                    +-- per pixel --------------------------+
                    |  Ray(eye, dir)   -> normalized()      |
                    |     |                                 |
                    |     v                                 |
                    |  8 x intersectionTime()               |
                    |     |                                 |
                    |     v  nearest hit                    |
                    |  colourAt()                           |
                    |     |-- reflection ray -> rayColour()  |  (depth <= 3)
                    |     +-- 2 lights x visibleLights()     |
                    |            +-- 8 x intersectionTime()  |
                    +---------------------------------------+


================================================================================
2. INITIAL ANALYSIS
================================================================================

METHODOLOGY
-----------
Same approach as pyflate, and shaped by the same guest constraints (see
report_pyflate.txt section 2 for the full findings):

  py-spy          WHERE the time goes, per PYTHON function.  Used because
                  CPython 3.10 predates the -X perf trampoline, so perf cannot
                  name Python functions in this guest.
  cProfile        call counts, which a sampler cannot give.
  perf stat       WHY: cycles, IPC, cache and branch behaviour.  Works in the
                  guest only because it was launched with -cpu host,pmu=on, and
                  only in groups of two events at a time.
  perf record     flame graphs at the C level, using -e cpu-clock; sampling a
                  hardware event captures zero samples in this guest.

BASELINE PROFILE (py-spy, Python-level, inside the guest)
----------------------------------------------------------
From results_qemu/flamegraph_top.txt:

    dot                                           18.58%
    __sub__                                       15.71%
    __init__                                      15.19%
    intersectionTime                              11.81%
    _lightIsVisible                                8.00%
    scale                                          4.42%
    <listcomp>  (rayColour's candidate list)       3.30%
    normalized                                     3.06%
    magnitude                                      2.78%

  Grouped: the Vector/Point wrappers - dot, __sub__, __init__, scale,
  normalized, magnitude - account for 59.74% of runtime.  Nearly SIX TENTHS of
  the program is spent in three-float arithmetic wrappers, not in the ray
  tracing algorithm.

CALL COUNTS (cProfile)
----------------------
  3,086,307 function calls for one 100x100 frame - 309 calls per pixel.
    509,871  Vector.dot
    277,865  Point.__sub__
    179,457  Sphere.intersectionTime

WHAT THE PROFILE ACTUALLY SAYS
------------------------------
  1. Sphere.intersectionTime is called 179,457 times for 10,000 pixels - about
     18 intersection tests per pixel, not 8.  The extra ones are shadow rays
     and reflection rays.  Intersection is THE kernel, and it is what the
     hardware proposal targets.

  2. Every one of the 509,871 Vector.dot calls begins with
     other.mustBeVector() - a method call whose only purpose is a type
     assertion.  Point.__sub__ similarly calls other.isPoint() 277,865 times.
     A large fraction of the "arithmetic" cost is dynamic type checking.  The
     py-spy grouping above makes this concrete: 59.74% of runtime is in these
     wrappers, and Vector.__init__ alone is 15.19%.

  3. Scene._lightIsVisible contains a loop-invariant expression:

         for (o, s) in self.objects:
             t = o.intersectionTime(Ray(p, l - p))      # <-- inside the loop

     Ray(p, l - p) does not depend on o.  Its constructor calls normalized(),
     which calls magnitude(), which calls sqrt(dot(self)).  So the SAME shadow
     ray - including its square root - is reconstructed 8 times per light per
     shading point.  This is a plain bug, not a subtle trade-off, and it shows
     up as _lightIsVisible at 8.00% plus much of the scale/normalized/magnitude
     time.

  4. Scene.rayColour allocates a list of 8 (object, t, surface) tuples for
     EVERY ray - primary, shadow and reflection alike - then scans it in
     firstIntersection().  That list comprehension is visible in the profile at
     3.30%; the allocator and GC cost it causes is spread across the rest.

  Additionally, no class defines __slots__, so every Vector, Point and Ray
  carries a per-instance __dict__ - and the renderer creates several per pixel.

FLAME GRAPHS
------------
  results_qemu/flamegraph_pyspy_raytrace_baseline.svg    Python-level
  results_qemu/flamegraph_pyspy_raytrace_optimized.svg
  results_qemu/flamegraph_perf_raytrace_baseline.svg     perf, cpu-clock
  results_qemu/flamegraph_perf_raytrace_optimized.svg

The py-spy baseline graph is a deep, narrow tower - the recursive rayColour ->
colourAt -> rayColour chain - resting on a very wide base of tiny Vector/Point
operator frames.  That "wide base of small frames" signature is the visual form
of finding 2: the leaves are type checks and operator dispatch, not
floating-point work.


================================================================================
3. OPTIMIZATIONS
================================================================================

CONSTRAINT: BIT-IDENTICAL OUTPUT
--------------------------------
The rendered image must be unchanged.  Floating-point addition is not
associative, so every expression below preserves the baseline's operand ORDER
and GROUPING exactly - the optimizations remove interpreter work, never
rearrange arithmetic.  tools/verify.py compares the full 30,000-byte RGB
framebuffer, and it was additionally checked at seven resolutions (2x2, 3x7,
16x16, 50x50, 100x100, 101x37, 128x96) so the default is not a special case.

O1 - HOIST THE SHADOW RAY OUT OF THE OBJECT LOOP
-------------------------------------------------
Construct Ray(p, l - p) once, before the loop over objects.  Identical result
(nothing in the loop mutates p or l), one sqrt instead of eight.

O2 - NO PER-RAY INTERSECTION LIST
----------------------------------
Track the running minimum inline instead of building a list of candidates and
scanning it.  The baseline's tie-breaking is preserved exactly: strict `<`
means the earliest object in the list wins a tie, so the same object is chosen.
Allocates nothing per ray.

O3 - REMOVE TYPE CHECKS FROM VECTOR ARITHMETIC
-----------------------------------------------
The hot paths know their operand types statically, so intersectionTime,
normalAt, normalized, reflectThrough and the Lambert loop operate directly on
raw floats: no dot(), no mustBeVector(), no temporary Vector objects.  The
public Vector/Point API keeps its semantics for any other caller.

O4 - __slots__ ON THE GEOMETRY CLASSES
---------------------------------------
Vector, Point, Ray, Sphere, Halfspace, Scene, Canvas and the surfaces all gain
__slots__: no per-instance __dict__, smaller objects, attribute access by fixed
offset instead of a dict lookup.  Given that Vector.__init__ alone was 15.19%
of baseline runtime, this is not a micro-optimization.

O5 - CHEAP ARITHMETIC AND ALLOCATION HOISTS
--------------------------------------------
Sphere caches radius*radius.  ycomp is computed once per scanline rather than
once per pixel (it depends only on y).  The Canvas blue channel is filled with
one slice assignment instead of a 30,000-iteration Python loop.

DELIBERATELY NOT DONE
---------------------
  * CheckerboardSurface.baseColourAt() calls v.scale(...) and DISCARDS the
    result, so checkSize never takes effect.  That is a real bug in the
    original, but fixing it would change the rendered image, and the brief
    requires identical output.  It is left exactly as-is and flagged here
    rather than silently preserved.
  * No numpy, no C extension: the benchmark measures the interpreter.
  * No BVH.  A bounding-volume hierarchy would beat all of the above for large
    scenes, but with 8 objects the traversal overhead exceeds the 8
    intersection tests it would save.  Worth stating explicitly, because it is
    the first thing a graphics person would propose - and here it is wrong.

TWO API DIVERGENCES, DISCLOSED
------------------------------
Removing the type assertions (O3) means Vector - Point and Vector.dot(Point)
now compute silently where the baseline raised TypeError.  Both are invalid
usage the renderer never produces, but they are a behaviour change, so they are
documented rather than left to be discovered.


================================================================================
4. PERFORMANCE COMPARISON
================================================================================

END-TO-END (pyperf --rigorous, inside the guest, taskset -c 2)
---------------------------------------------------------------
    baseline    809.3 ms +- 12 ms
    optimized   282.2 ms +- 14 ms
    ------------------------------------------------
    runtime cut 65.1%      speedup 2.87x

    pyperf compare_to -v:
      Mean +- std dev: [raytrace_baseline] 809 ms +- 12 ms
                    -> [raytrace_optimized] 282 ms +- 14 ms: 2.87x faster
      Significant (t=315.11)

    The requirement is >= 7%.  This is 65.1%.

HARDWARE COUNTERS - THE "WHY" (perf stat, groups of two, 3 iterations each)
---------------------------------------------------------------------------
                          baseline          optimized       ratio
    cycles              5,947,773,848     2,047,210,838     2.91x fewer
    instructions       15,393,620,380     5,529,235,780     2.78x fewer
    IPC                          2.59              2.70     +4%
    cache-references       18,674,862         2,997,893     6.23x fewer
    cache-misses              189,730            87,106     2.18x fewer
      (miss rate)               1.02%             2.91%
    branch-instructions 2,972,405,286     1,035,375,368     2.87x fewer
    branch-misses          10,430,884         4,544,055     2.30x fewer

READING THIS TABLE
------------------
As with pyflate, cycles fell in step with the stopwatch - 2.91x against a 2.87x
runtime improvement - while IPC moved only 2.59 -> 2.70.  Instructions retired
fell 2.78x.  So the speedup decomposes as 2.78 x 1.04 = 2.89: almost entirely
"execute fewer instructions", with a 4% efficiency bonus.  Every
mustBeVector() call that disappeared was a real call - argument tuple, frame
push, return.

One row deserves attention because it looks like a regression:

    cache-miss RATE went UP, 1.02% -> 2.91%

while absolute misses fell 2.18x.  This is the same phenomenon the course's own
perf tutorial demonstrates with row-major versus column-major traversal: the
optimized code makes 6.23x fewer cache REFERENCES, because it is not allocating
and touching a swarm of short-lived Vector objects.  The references that remain
are the genuinely cold ones, so the ratio of misses to references rises even
though the machine stalls far less often.  A miss RATE in isolation is not a
performance metric; misses per unit of work is.

The effect is more pronounced in the guest than it was on the host (1.02 ->
2.91 versus 1.20 -> 1.40), which makes the teaching point sharper rather than
weaker: the same optimization can move a rate in the "wrong" direction by
different amounts on different machines, while always reducing the absolute
count.

CALL-COUNT COLLAPSE
-------------------
    baseline    3,086,307 Python function calls
    optimized     616,472 Python function calls
    ------------------------------------------------
    5.01x fewer calls

    Sphere.intersectionTime is still called 179,457 times - the ALGORITHM is
    unchanged - but it no longer spawns a cascade of dot(), __sub__() and
    mustBeVector() frames beneath it.

PER-OPTIMIZATION ATTRIBUTION (tools/ablate.py, best of 5, in the guest)
------------------------------------------------------------------------
Each row reverts ONE optimization from the fully optimized build.  Marginal
costs do not sum to the total; the optimizations interact.  Read as a ranking.

    variant                                best (ms)   marginal cost
    ------------------------------------   ---------   --------------
    baseline (all optimizations off)           797.8
    fully optimized                            277.8
      without O3/O5 (inlined intersection)     432.8   +155.0 ms (+56%)
      without O1  (shadow-ray hoist)           399.2   +121.4 ms (+44%)
      without O2  (per-ray list alloc)         300.9   +23.1 ms (+8%)

    Two results are worth pointing at in the presentation:

      * O1 - deleting one loop-invariant expression - is worth 44%.  It is a
        three-line change with no cleverness in it.  Profiling found it;
        reading the code at normal speed would not have.
      * O2, removing ~200k allocations per frame, is worth only 8%.  The
        intuition that "allocation is the problem" is measurably wrong here;
        type-check and call overhead cost seven times more.

PROFILE AFTER OPTIMIZATION
--------------------------
    intersectionTime                              35.03%
    rayColour                                     10.91%
    _lightIsVisible                                8.12%
    colourAt                                       7.11%
    __init__                                       6.85%
    plot                                           6.35%
    render                                         5.58%

  The wide base of Vector/Point wrappers is gone and intersectionTime is now
  the single dominant function at 35% - which is exactly the kernel the
  hardware accelerator targets, and the empirical justification for choosing it.

CORRECTNESS
-----------
    root@ubuntu:~/hwsw# python3 tools/verify.py raytrace
    raytrace (100x100)
      baseline  : 30000 bytes, md5 d7c5cc8679a87caf9ae61b8446509254
      optimized : 30000 bytes, md5 d7c5cc8679a87caf9ae61b8446509254
      PASS: bit-identical image


================================================================================
5. HARDWARE ACCELERATION PROPOSAL
================================================================================

TARGET AND DESIGN PATTERN
-------------------------
Target: ray-sphere intersection - 35.03% of the optimized runtime and the
kernel the ablation ranks first (+56%).

Unlike pyflate's Huffman decoder, this kernel has FIXED operand count, FIXED
latency and NO data-dependent control flow.  Every test computes exactly:

    c    = centre - ray.point                 3 subtractions
    v    = c . ray.vector                     3 mul + 2 add
    cc   = c . c                              3 mul + 2 add
    disc = r^2 - (cc - v*v)                   1 mul + 2 sub
    t    = v - sqrt(disc)                     1 sqrt + 1 sub

That regularity is what a SYSTOLIC ARRAY wants, and it is why this design
differs fundamentally from the pyflate accelerator:

    pyflate   variable-length codes -> data-dependent rate -> DATAFLOW
    raytrace  fixed-shape FP kernel  -> fixed cadence       -> SYSTOLIC ARRAY

Sphere parameters are WEIGHT-STATIONARY: (cx, cy, cz, r^2) are preloaded into
the PEs once per frame and stay there while rays stream through - structurally
the same trick as the TPU preloading weights and streaming activations.

BLOCK DIAGRAM
-------------

    HOST (CPU)                          RAY-SPHERE INTERSECTION ARRAY
  +------------------+
  |  Python renderer |          weights (preloaded once per frame)
  |   scene.render() |                 |
  +--------+---------+                 v
           |               +-----------------------------------------+
           | scene upload  |  PE0        PE1        PE2   ...   PE7   |
           +-------------->|  +------+   +------+   +------+  +------+|
                           |  |cx cy |   |cx cy |   |cx cy |  |cx cy ||
                           |  |cz r2 |   |cz r2 |   |cz r2 |  |cz r2 ||
    ray stream             |  +--+---+   +--+---+   +--+---+  +--+---+|
  (px,py,pz,vx,vy,vz) ---->|     |          |          |         |    |
      + ray_ready          |     v          v          v         v    |
                           |  [3 sub]    [3 sub]    [3 sub]   [3 sub] |
                           |  [6 mul]    [6 mul]    [6 mul]   [6 mul] |
                           |  [4 add]    [4 add]    [4 add]   [4 add] |
                           |     |          |          |         |    |
                           |   (v,disc)  (v,disc)  (v,disc)  (v,disc) |
                           |     +----------+-----+----+---------+    |
                           |                      |                   |
                           |                      v                   |
                           |            +--------------------+        |
                           |            |   capture bank     |        |
                           |            |  latches all N,    |        |
                           |            |  then serializes   |        |
                           |            +---------+----------+        |
                           |                      v                   |
                           |            +--------------------+        |
                           |            |  fp32_sqrt         |        |
                           |            |  24-stage pipeline |        |
                           |            |  SHARED            |        |
                           |            +---------+----------+        |
                           |                      v                   |
                           |            [ t = v - sqrt(disc) ]        |
                           |              v aligned by delay line     |
                           |                      v                   |
                           |            +--------------------+        |
                           |            | nearest-hit reduce |        |
                           |            | per-ray accumulator|        |
                           |            | strict < tie-break |        |
                           |            +---------+----------+        |
                           +----------------------|------------------+
                                                  v
                                    (t_nearest, obj_id, hit) -> host

INTERFACES: INPUTS AND OUTPUTS
------------------------------
  Clock / reset       clk, rst_n.  Target 1 GHz in a 16 nm-class process.
                      Critical path is inside fp32_add's normalize shifter
                      (leading-zero count on a cancelling subtraction), which
                      is why the datapath is a MAC chain rather than a wide
                      adder tree.

  Weight load         w_load, w_idx[7:0], w_cx/w_cy/w_cz/w_r2 [31:0]
                      One sphere per cycle; 8 cycles to load this scene.

  Ray stream          px,py,pz,vx,vy,vz [31:0], ray_valid; ray_ready back to
                      the host.  192 bits/ray.  The array accepts one ray every
                      N_SPHERES cycles, which is what the shared sqrt allows -
                      ray_ready makes that contract explicit rather than
                      implicit.

  Result              t_nearest[31:0], obj_nearest[7:0], hit_any, result_valid

  Numeric format      IEEE-754 binary32.  Round-to-nearest-even on mul/add;
                      subnormals flushed to zero (standard for a graphics
                      datapath - it removes the subnormal shifter from the
                      critical path for values that cannot move an 8-bit
                      colour channel).

  Latencies           fp32_mul  2 cycles, throughput 1/cycle
                      fp32_add  2 cycles, throughput 1/cycle
                      fp32_sqrt 24 stages, 25-cycle latency, 1/cycle throughput
                      PE end-to-end ~14 cycles, fully pipelined

INTERNAL ARCHITECTURE
---------------------
Each ray_sphere_pe holds one sphere and computes (v, disc, hit) for the ray
passing it, using 3 fp32_add for the centre subtraction, 6 fp32_mul and 4
fp32_add for the two dot products, then 1 mul and 2 adds for the discriminant.

All N PEs finish on the SAME cycle, and the shared sqrt can accept only one
candidate per cycle, so a CAPTURE BANK latches all N results and serializes
them.  Reading the PE outputs live across N cycles would sample N different
rays instead of the N spheres of one ray - that was a real bug in an earlier
revision and is described in section 7.

The square root is NOT replicated per PE.  fp32_sqrt is 24 pipeline stages -
by far the largest block - and one fully pipelined instance retires one
candidate per cycle.  With N_SPHERES = 8 the sqrt therefore sets throughput at
one ray per 8 cycles.  This is the central area/performance decision, quantified
below.

fp32_sqrt uses the classic restoring bit-by-bit integer square root on the
significand, unrolled into 24 stages.  Radicand scaling:
    E even -> radicand = {1,m} << 23, root = sqrt(1.m)   * 2^23, exp = E/2
    E odd  -> radicand = {1,m} << 24, root = sqrt(2*1.m) * 2^23, exp = (E-1)/2
Either way the root lands in [2^23, 2^24), already normalized, so no
post-normalize shifter is needed.

VERIFICATION - RUN IN THE GUEST, AND IT FOUND A REAL BUG
---------------------------------------------------------
  1. sqrt golden model (hw/raytrace_mac/sqrt_model.py), 6,008 samples:

         24 stages, truncating (as built)   worst 1.16 ulp
         25 stages + round-to-nearest       worst 0.69 ulp
         sqrt(1), sqrt(4), sqrt(9), sqrt(16), sqrt(0.25)  all bit-exact

     The as-built unit truncates, so it is NOT bit-identical to math.sqrt().
     That is a deliberate trade (it removes a 24-bit incrementer from the tail)
     and it is why a hardware-vs-software image comparison here needs a
     tolerance, in contrast to the pyflate accelerator, which is bit-exact.

  2. RTL simulation, Icarus Verilog 11.0, inside the QEMU guest:

         tb_ray_sphere  (single PE)
           ok  fp32_mul 2*3 = 40c00000
           ok  fp32_add 2+3 = 40a00000
           ok  fp32_add 9-4 = 40a00000
           ok  fp32_sqrt(4) = 40000000
           ok  fp32_sqrt(9) = 40400000
           ok  pe v    = 41200000   (10.0)
           ok  pe disc = 40800000   ( 4.0)
           TB PASS: all checks bit-exact

         tb_ray_array  (array level, 8 spheres at t = 18, 8, 12 and 5 misses)
           ok  hit_any = 1
           ok  t_nearest = 41000000 (8.0, the nearest sphere)
           ok  obj_nearest = 1
           TB PASS: nearest-hit selection correct across 8 spheres

         tb_fp_random
           TB PASS: 400 random vectors, add and mul both bit-exact

     The worked single-PE case, also computable by hand:
         sphere centre (0,0,-10), r=2 -> r^2 = 4
         ray    origin (0,0,  0), dir (0,0,-1)
         c = (0,0,-10);  v = 10;  cc = 100
         disc = 4 - (100 - 100) = 4
         t    = 10 - sqrt(4) = 8
     Every intermediate is exactly representable in binary32, so it must be
     bit-exact end to end - no tolerance needed.

  THE BUG.  The FIRST run of tb_ray_sphere failed: 2+3 produced 6.5 and 9-4
  produced 2.5.  fp32_add had two coupled off-by-one errors - the carry path
  normalized the leading one to bit 26 while the leading-zero path normalized
  it to bit 27, and the mantissa slice sum_n[26:4] matched neither convention.
  Fixed to a single bit-26 convention with mant_raw = sum_n[25:3].

  This is the honest value of simulating rather than asserting: the module had
  been written carefully, reviewed, and was completely wrong on its second test
  vector.  tb_fp_random was added afterwards precisely because three
  hand-picked cases are not enough to trust a floating-point unit - a quarter
  of its 400 vectors are near-cancelling subtractions, the case that exercises
  the long normalize path where the bug lived.

ACCELERATION JUSTIFICATION AND EXPECTED SPEEDUP
-----------------------------------------------
Why this component: 35.03% of the optimized runtime, 179,457 calls per frame of
identical shape, and embarrassingly parallel across objects.

Estimate, with assumptions stated:

    Intersection tests per frame            179,457
    Array: 1 ray (8 spheres) / 8 cycles     ~22,432 ray-groups
    At 1 GHz                                ~22 us of array time

    Optimized software (in the guest)       282 ms
    Baseline software (in the guest)        809 ms

Assumptions: rays are generated on-chip or streamed at full rate, shading and
the recursive control flow still run on the host, and transfer overhead is
excluded.  The honest caveat is Amdahl: intersection is 35.03% of the optimized
runtime, so even an infinitely fast intersection unit caps the whole-frame
speedup at 1/(1-0.3503) = 1.54x.  Quoting the ~12,000x kernel ratio without
that caveat would be meaningless.  Accelerating the shading path as well is
what would be needed for a real product - which is the path GPUs actually took.

HW/SW INTERFACE
---------------
  Rule 1 - users do not change their code.  A renderer still calls
      scene.render(); the accelerator sits under the intersection primitive.
  Rule 2 - changes confined to the library.  The geometry layer gains a
      batched intersect(rays, spheres) entry point; the scene uploads once per
      frame.
  Rule 3 - never break the user's code.  The array handles spheres only.
      Halfspaces, and any scene with more objects than PEs, fall back to the
      CPU path - which is why the software keeps a working scalar
      implementation.

  Batching is essential, not incidental: one ray per transaction would be
  dominated by descriptor overhead.  The driver accumulates a tile of rays and
  issues them as one DMA, the same reason GPUs shade in warps.  ray_ready tells
  it when the array can take the next one.

PERFORMANCE / AREA / POWER TRADE-OFFS
-------------------------------------
  The sqrt/area knob, quantified:

    shared sqrt (as built)   1 sqrt unit    1 ray / 8 cycles   smallest area
    one sqrt per PE          8 sqrt units   1 ray / 1 cycle    ~8x sqrt area

  fp32_sqrt at 24 stages x ~50 bits of pipeline registers is roughly comparable
  in area to the entire arithmetic datapath of one PE, so replicating it
  approximately doubles total array area for an 8x throughput gain.  For a
  throughput-limited renderer that is a good trade; for an area-limited edge
  part it is not.  The design is parameterized so both points are buildable.

  Accuracy vs area, also quantified (sqrt_model.py): the 25th stage plus a
  24-bit rounding incrementer buys 1.16 ulp -> 0.69 ulp.  For shading that
  precision is not needed, so the cheaper unit ships.

  Scaling N_SPHERES: area grows linearly, but so does the serializer depth
  feeding the shared sqrt, so throughput per ray stays at N cycles.  Beyond
  ~16 spheres a second sqrt unit is a better spend than more PEs.

  Precision: dropping to bfloat16 for the dot products would roughly halve
  multiplier area and power.  It is rejected - 8 mantissa bits is not enough
  for a discriminant that is the difference of two large, nearly equal numbers
  (cc - v*v), which is exactly where catastrophic cancellation lives.  This is
  the one place in the design where precision is not negotiable.  Note that the
  fp32_add bug above lived in precisely this cancellation path, which is a
  useful reminder of how delicate it is.

  Power: the multipliers dominate dynamic power.  Clock-gating a PE whose
  sphere is behind the ray origin (v < 0, known early) skips most of the
  datapath for a large share of tests in a typical scene.


================================================================================
6. CONCLUSION
================================================================================

The optimized renderer is 2.87x faster than the baseline inside the course QEMU
guest and produces a bit-identical image, verified over the full 30,000-byte
framebuffer and at seven resolutions.

The most valuable finding was not clever: Scene._lightIsVisible rebuilt the same
shadow ray - square root included - once per object, and hoisting that one
loop-invariant expression is worth 44% on its own.  The second was that the
tiny Vector/Point wrappers, and the 509,871 mustBeVector() type assertions
inside them, accounted for 59.74% of runtime - more than the arithmetic they
were guarding.  Both came from the profile; neither is visible from reading the
code at normal speed.  The counterpoint is equally instructive: eliminating
~200,000 allocations per frame, the thing one instinctively reaches for, was
worth only 8%.

The hardware counters confirm the mechanism rather than merely the result:
cycles fell 2.91x against a 2.87x speedup, instructions fell 2.78x, and IPC
moved only 2.59 -> 2.70.  And the cache-miss RATE rising from 1.02% to 2.91%
while absolute misses fell 2.18x is the same trap the course's own perf
tutorial sets with row- versus column-major traversal - a rate is not a
performance metric.

In hardware this kernel is the systolic counterpart to pyflate's dataflow
decoder.  Fixed operand count, fixed latency and no data-dependent control make
a weight-stationary array of intersection PEs the natural structure, with
sphere parameters preloaded and rays streamed through exactly as the TPU
preloads weights.  The interesting engineering decision is not the multipliers
but the square root: sharing one 24-stage pipeline across 8 PEs costs 8x
throughput and saves roughly half the array area.

Simulating the RTL rather than merely writing it was worth doing: fp32_add was
wrong on its second test vector, in the cancellation path that the precision
analysis above identifies as the most delicate part of the whole datapath.  And
reviewing the array around it found four more alignment bugs that the PE-level
testbench could not see.

The honest limit is Amdahl's law.  Intersection is 35% of the optimized
runtime, so accelerating it perfectly caps the frame speedup at 1.54x.  A
useful product would have to move shading onto the accelerator as well.


================================================================================
7. CRITICAL REVIEW - KNOWN LIMITATIONS AND WHAT TESTING FOUND
================================================================================

This section exists because "it passed" is not the same as "it is correct".
Everything below was found by deliberately attacking the implementation after
it was written, and is stated rather than quietly fixed or hidden.

BUGS FOUND AND FIXED
--------------------
  fp32_add normalize  -  FOUND BY SIMULATION, FIXED
      Failed on its second test vector: 2+3 gave 6.5, 9-4 gave 2.5.  The
      carry-out path normalized the leading one to bit 26 while the
      leading-zero path normalized to bit 27, and the mantissa slice matched
      neither.  Fixed to one bit-26 convention, mant_raw = sum_n[25:3].

  Fake valid signalling  -  FOUND BY REVIEW, FIXED
      fp32_mul and fp32_add tied out_valid to 1 and discarded in_valid, so
      every downstream handshake was meaningless - and it MASKED the four bugs
      below, because stale PE outputs happened to look valid.  Valid now
      propagates one register per pipeline stage.

  Four array-level alignment bugs  -  FOUND BY REVIEW, FIXED
      ray_sphere_array computed t as v_arr[0] - sqrt(disc) for EVERY candidate,
      so only sphere 0 ever got the right t; carried no identity across the
      25-cycle sqrt, so obj_nearest was unrelated to the winner; advanced its
      index counter only on hits, so a miss desynchronized it; and never
      re-initialized the running minimum, so after the first ray it reported a
      global minimum.  Fixed with an aligned delay line and a per-ray
      accumulator.  tb_ray_array.v now checks that the NEAREST of 8 spheres
      wins and is named correctly - the test that would have caught all four.

  Capture bank missing  -  FOUND BY REVIEW, FIXED
      All N PEs produce their results on the same cycle, but the serializer
      read them live over N cycles - sampling N different rays instead of the
      N spheres of one ray.  Results are now latched, and a ray_ready handshake
      tells the host the array accepts one ray every N cycles.

  Testbench sampled at the wrong time  -  FOUND BY REGRESSION, FIXED
      tb_ray_sphere read the PE outputs at an arbitrary later cycle, which was
      only valid while out_valid was fake.  Once valid became real, `hit` read
      0.  The testbench now latches on out_valid, which is how a pipelined unit
      with a valid signal must be sampled.

KNOWN LIMITATIONS - STATED, NOT FIXED
-------------------------------------
  1. fp32_sqrt truncates, so this accelerator is NOT bit-identical to
     math.sqrt() - 1.16 ulp worst case over 6,008 samples.  A
     hardware-vs-software image comparison therefore needs a tolerance.  The
     pyflate accelerator IS bit-exact.

  2. Subnormal inputs and outputs are flushed to zero in the FP units.  A scene
     pushing into the subnormal range would diverge from the software
     reference.

  3. Two API divergences from removing the type assertions: Vector - Point and
     Vector.dot(Point) raise TypeError in the baseline but compute silently
     now.  Invalid usage the renderer never produces.

  4. Amdahl caps the accelerator at 1.54x on a whole frame, because intersection
     is 35% of the optimized runtime.  Shading would have to move on-chip too.

  5. No CPI stack in the guest: stalled-cycles-frontend and
     stalled-cycles-backend are <not supported>, so the hierarchical stall
     breakdown from lecture 4 cannot be reproduced inside the VM.

VERIFICATION STATUS
-------------------
  tools/regress.sh runs all nine checks.  All nine pass inside the QEMU guest
  under Icarus Verilog 11.0 - see report_pyflate.txt section 7 for the list.

A NOTE ON WHERE THIS WAS MEASURED
---------------------------------
An earlier revision of this report carried numbers measured on the naranja7
HOST rather than inside the guest.  That violated the course rule that all work
happens in the provided QEMU image.  Everything was re-measured inside the
guest and every figure replaced.  The host numbers are retained only in
results_naranja7/ as a labelled cross-check - the speedup ratios turning out to
be platform-independent (2.55x host vs 2.87x guest) is worth knowing, but they
are not the submitted result.
