================================================================================
  report_pyflate.txt
  HW/SW Co-design (00460882) - Final Project
  Benchmark 1 of 2: pyflate  (pure-Python bzip2 decompressor)

  Taha Taha  and  Lana Bakrieh
  Repository: https://github.com/taha-tahaa/hwsw-codesign-project
================================================================================

  WHERE THESE NUMBERS COME FROM - INSIDE THE COURSE QEMU IMAGE
  ------------------------------------------------------------
  Every figure in this report was measured INSIDE the provided QEMU guest, as
  the course requires.  Nothing here was measured on the naranja host.

    virtualization  systemd-detect-virt = kvm        <- confirms the VM
    distro          Ubuntu 22.04.5 LTS
    kernel          5.15.0-1080-kvm
    vcpus / memory  8 / 3938 MB
    host cpu        Intel Xeon E5-2630 v3 @ 2.40GHz (guest PMU: haswell)
    python          3.10.12      pyperf 2.10.0      perf 5.15.179
    iverilog        11.0         py-spy 0.4.2
    pinning         taskset -c 2

  The guest was launched from /scratch/$USER on the assigned naranja host with:

    qemu-img create -f qcow2 -b jammy-server-cloudimg-amd64-disk-kvm.img \
                    -F qcow2 work.qcow2
    qemu-system-x86_64 -m 4096 -smp 8 -cpu host,pmu=on -accel kvm \
        -nographic -nic user,model=virtio-net-pci \
        -drive file=work.qcow2,format=qcow2

  Two details of that command matter:

    pmu=on        Without it the guest has NO virtual PMU and perf answers
                  <not supported> for every hardware event.  With it, cycles /
                  instructions / cache / branch counters all work, which is why
                  this report can carry real counter data at all.

    work.qcow2    A qcow2 OVERLAY over the course image, so
                  jammy-server-cloudimg-amd64-disk-kvm.img is never written to.

  Full record: results_qemu/environment.txt.  Reproduce: ./script_pyflate.sh


  REQUIREMENT CHECK - >= 7% ON TWO OR MORE BENCHMARKS
  ---------------------------------------------------
  Project.pdf, instruction 6: "If you can identify two or more benchmarks where
  your optimizations result in a performance improvement of at least 7%, that
  will be considered sufficient."

  +------------+------------------+-----------------+-----------+---------+---------------------+
  | Benchmark  | Baseline         | Optimized       | Runtime   | Speedup | Significance        |
  |            |                  |                 | cut       |         |                     |
  +============+==================+=================+===========+=========+=====================+
  | pyflate    | 1115.1 ms +- 10  | 444.6 ms +- 6   |  60.1%    |  2.51x  | Significant t=676.00|
  +------------+------------------+-----------------+-----------+---------+---------------------+
  | raytrace   |  809.3 ms +- 12  | 282.2 ms +- 14  |  65.1%    |  2.87x  | Significant t=315.11|
  +------------+------------------+-----------------+-----------+---------+---------------------+

  "7% improvement" is ambiguous between two readings; both are satisfied:
      as a RUNTIME CUT   60.1% and 65.1%   (7% required)
      as a SPEEDUP       2.51x and 2.87x, i.e. 151% and 187% faster (1.07x needed)

  And neither is bought by changing behaviour: pyflate output is byte-identical
  over all 399,360 decompressed bytes, raytrace bit-identical over the whole
  30,000-byte framebuffer and at seven different resolutions.

  Reproduce:
      python3 -m pyperf compare_to results_qemu/<bench>_baseline.json \
                                   results_qemu/<bench>_optimized.json -v


================================================================================
1. OVERVIEW
================================================================================

WHAT THE BENCHMARK DOES
-----------------------
pyflate is Paul Sladen's stand-alone pure-Python DEFLATE (gzip) and bzip2
decoder.  pyperformance drives it over data/interpreter.tar.bz2 - 67,562
compressed bytes that expand to 399,360 bytes - and checks the result against a
built-in MD5 (afa004a630fe072901b1d9628b960974).  That checksum is the
correctness oracle used throughout this project.

The workload is bzip2 only (magic 0x425a).  The gzip path is never exercised.

LIBRARIES USED
--------------
Almost none, and that is the point.  The benchmark deliberately implements
decompression in Python rather than calling libbz2, so it measures the
interpreter executing bit-manipulation and table-lookup code.  Imports are
hashlib (checksum), struct (int2byte), os, and pyperf (timing).

This also rules out an obvious cheat: swapping in libbz2 would post a huge
"speedup" while measuring nothing and teaching nothing.  We did not do it.

ALGORITHM AND DATA STRUCTURES
-----------------------------
A bzip2 block is decoded in four stages, in this order:

  1. Huffman decode      variable-length prefix codes -> symbol indices
  2. Move-to-front (MTF) symbol indices -> byte values, with the alphabet
                         reordered after each hit
  3. Inverse BWT         un-permute the block via a pointer chain
  4. RLE                 expand runs of >= 4 identical bytes

Baseline data structures:

  BitfieldBase / Bitfield / RBitfield
      Bit cursor over a file object.  RBitfield is MSB-first and is the one
      bzip2 uses.  Reads accumulate one BYTE at a time via f.read(1).

  HuffmanLength / HuffmanTable / OrderedHuffmanTable
      A Python list of HuffmanLength objects sorted by (bits, code).
      populate_huffman_symbols() assigns canonical MSB-first codes.

  favourites
      The MTF alphabet: a list of 1-byte bytes objects, reordered per symbol.

  bwt_transform / bwt_reverse
      T[] is a Python list of ints; the block is rebuilt by chasing
      end = T[end] and appending L[end] to a list.


================================================================================
2. INITIAL ANALYSIS
================================================================================

WHAT perf CAN AND CANNOT DO IN THIS GUEST
-----------------------------------------
The measurement method had to be built around what actually works inside the
VM.  Three things were established by testing, not assumed:

  1. HARDWARE COUNTING WORKS - because of one launch flag.
     With -cpu host,pmu=on the guest gets a Haswell vPMU and cycles,
     instructions, cache-references, cache-misses, branch-instructions and
     branch-misses all count correctly.  Without the flag every one of them
     reads <not supported>.

  2. HARDWARE SAMPLING DOES NOT WORK.
     `perf record` on the default cycles event captures ZERO samples in this
     guest, even though `perf stat -e cycles` counts that same event fine.
     Counting and sampling are different paths, and only counting is
     virtualized here.  Flame graphs therefore use -e cpu-clock, which is
     timer-based and works.

  3. FEWER USABLE COUNTERS THAN A BARE MACHINE.
     Requesting six events in one `perf stat` run makes one of them read
     exactly 0.  That looks like "cycles is unsupported in the guest" and is
     not - it is counter multiplexing.  Every counter figure below was
     therefore taken in GROUPS OF TWO.  This was a real trap: the first
     measurement pass reported cycles = 0 and would have been written up as a
     guest limitation.

  4. perf CANNOT NAME PYTHON FUNCTIONS HERE.
     CPython 3.10 predates the -X perf trampoline (3.12+), so perf's flame
     graphs show CPython internals - _PyEval_EvalFrameDefault and friends.
     Python-level attribution comes from py-spy, which walks CPython's frame
     objects itself and needs no interpreter support, cross-checked against
     cProfile.

  5. NO CPI STACK.
     stalled-cycles-frontend and stalled-cycles-backend are genuinely
     <not supported> in the guest, so the hierarchical stall breakdown from
     lecture 4 cannot be built here.

  A note on the brief's suggested command,
      perf record -F 999 -g -- python3-dbg -m pyperformance run --bench ...
  pyperformance builds a virtualenv and re-execs the benchmark in a CHILD
  process using an interpreter that is not python3-dbg unless --python is
  passed, so perf samples process machinery rather than the decoder.
  tools/profile_target.py runs the workload in-process instead, so every
  sample belongs to the code under study.

BASELINE PROFILE (py-spy, Python-level, inside the guest)
----------------------------------------------------------
From results_qemu/flamegraph_top.txt:

    decode_huffman_block                          21.79%
    find_next_symbol                              16.54%
    move_to_front                                 15.69%
    readbits                                       9.71%
    snoopbits                                      9.25%
    bwt_transform                                  9.01%
    bwt_reverse                                    6.16%
    _mask                                          3.70%
    _more                                          2.83%

  Grouped by stage:
    Huffman decode (find_next_symbol + readbits + snoopbits + _mask + _more)
                                                  42.0%
    Move-to-front                                 15.7%
    Inverse BWT (bwt_transform + bwt_reverse)     15.2%
    decode loop body                              21.8%

CALL COUNTS (cProfile, for the counts a sampler cannot give)
-------------------------------------------------------------
  3,185,454 function calls to decompress 400 KB.  Of these:
    148,271  find_next_symbol
    341,601  snoopbits
    655,017  _mask
    156,708  readbits
     92,803  move_to_front
    766,811  list.append
     67,562  _read   (one buffered f.read(1) per compressed byte)

WHAT THE PROFILE ACTUALLY SAYS
------------------------------
The headline is NOT "Huffman decoding is slow".  Four findings drove the work:

  1. THE LINEAR SCAN IS NOT THE PROBLEM.
     find_next_symbol walks a (bits, code)-sorted table, which invites "the
     scan is too deep".  But 148,271 calls to it produced only 341,601
     snoopbits calls - a ratio of 2.3, not ~250.  Move-to-front keeps the hot
     symbols at short code lengths, so the scan almost always exits after two
     probes.
     => The cost is not scan depth.  Decoding ONE symbol costs about seven
        Python function calls: find_next_symbol, ~2.3 snoopbits, a readbits,
        and several _mask.
     => The fix must remove CALLS, not iterations.  This is the single most
        important observation in the analysis, and it is the opposite of what
        the benchmark's own source comment ("room for improvement in the
        Huffman bit-matcher") suggests at first reading.

  2. SAMPLING AND INSTRUMENTATION DISAGREE ABOUT move_to_front.
     py-spy puts it at 15.69% - third overall.  cProfile charges it 0.223 s of
     2.215 s, about 10.1%.  Instrumentation charges per CALL; sampling charges
     per CYCLE, and move_to_front is a function that is simply slow inside: it
     rebuilds a list from three slices and two concatenations on every one of
     its 92,803 invocations.  A call-count profiler under-reports exactly that
     shape.  This is why the lecture insists on sampling profilers rather than
     instrumentation alone, and it is why the optimization list below treats
     MTF as a first-class target rather than bookkeeping.

  3. _mask IS CALLED 655,017 TIMES to compute (1 << n) - 1, an expression that
     depends only on n, which takes about 20 distinct values.  And the stream
     is consumed one BYTE at a time through _read -> f.read(1): 67,562
     Python-level buffered reads.

  4. THE INVERSE BWT IS A TEXTBOOK IRREGULAR ACCESS PATTERN.
     end = T[end] is a dependent load with no spatial locality,
     un-prefetchable, exactly the case the memory-hierarchy lecture calls out.
     bwt_transform additionally built its histogram with sorted(L) plus 256
     bytes.find() scans of the block.

FLAME GRAPHS
------------
  results_qemu/flamegraph_pyspy_pyflate_baseline.svg    Python-level
  results_qemu/flamegraph_pyspy_pyflate_optimized.svg
  results_qemu/flamegraph_perf_pyflate_baseline.svg     perf, cpu-clock, C frames
  results_qemu/flamegraph_perf_pyflate_optimized.svg

The py-spy baseline graph is a wide, shallow plateau under
decode_huffman_block, with find_next_symbol and its snoopbits/readbits/_mask
children spread across most of the width - the visual signature of one hot loop
calling small functions far too often.  The optimized graph collapses to two
towers, decode_huffman_block and bwt_reverse.

The perf graphs are shown alongside deliberately: they are what the brief's
suggested command produces in this guest, and comparing the two makes the
tooling limitation concrete rather than abstract.


================================================================================
3. OPTIMIZATIONS
================================================================================

All optimizations preserve byte-identical output.  tools/verify.py decodes with
both implementations and compares the full 399,360-byte result plus its MD5;
script_pyflate.sh runs it as a gate before any timing is taken.

O1 - CANONICAL HUFFMAN DECODE (replaces the linear table scan)
--------------------------------------------------------------
populate_huffman_symbols() already assigns CANONICAL MSB-first codes, so the
table has algebraic structure the scan ignores.  build_decode_tables()
precomputes the standard limit/base/perm arrays once per Huffman table, after
which a symbol is decoded by:

    zvec = next min_len bits
    while zvec > limit[zn]: zn += 1; zvec = (zvec << 1) | next_bit
    sym  = perm[zvec - base[zn]]

O(codelen - min_len) cheap integer steps, no per-candidate object access, and -
crucially - no function calls.

O2 - INLINED BIT READER
-----------------------
The whole input is read once into a bytes object, and the bit cursor
(bitbuf, bitcnt, pos) is held in LOCAL VARIABLES inside decode_huffman_block.
Decoding a symbol now performs zero Python function calls.  The _Reader class
still exists for the cold path (headers, table setup) and hands its state to
and from the hot loop, so there is one implementation of the bit arithmetic.
The mask table is precomputed once rather than recomputed 655,017 times.

O1 and O2 are inseparable in measurement: the canonical decoder is what makes
inlining possible, and the inlining is what turns the algorithmic win into a
wall-clock win.

O3 - INVERSE BWT
----------------
Histogram computed directly with a 256-bin count plus a prefix sum, replacing
sorted(L) and 256 bytes.find() scans.  Reconstruction writes into a
preallocated bytearray by index instead of appending to a list.

O4 - MOVE-TO-FRONT
------------------
list.pop(i) + list.insert(0, v) performs the same reordering as a single
C-level memmove, replacing three slices and two concatenations.  The alphabet
is kept as ints rather than 1-byte bytes objects, removing an allocation per
emitted symbol.  Given finding 2 above, this matters considerably more than the
call-count profile implied.

O5 - OUTPUT ASSEMBLY
--------------------
Symbols accumulate into a bytearray instead of a list of ~767,000 one-byte
bytes objects joined at the end, and the final RLE pass indexes the block
directly rather than slicing per byte.

DELIBERATELY NOT DONE
---------------------
  * No C extension, no numpy, no ctypes - see "libraries used" above.
  * The gzip path is untouched and falls back to the baseline decoder, so no
    previously working input stops working - the "don't break the user's code"
    rule from the accelerator lecture, applied to software.
  * Randomised bzip2 blocks remain unsupported, exactly as in the baseline.

APPROACHES CONSIDERED AND REJECTED
----------------------------------
  * A full 2^20-entry lookup table per Huffman group would decode in one step,
    but bzip2 allows up to 6 tables of code length <= 20; that is ~6M entries
    to BUILD per block, for a block that decodes ~148k symbols.  Table
    construction would cost more than it saves.  A two-level LUT (fast 10-bit
    table + canonical fallback) is the sound middle ground and is the natural
    next step - it is also what the hardware does.
  * numpy for the inverse BWT: an added dependency for a stage the ablation
    shows is worth only ~3%.

TESTED BEYOND THE BENCHMARK FILE
--------------------------------
The benchmark only ever feeds one input, and tools/verify.py only proves that
the optimized decoder agrees with OUR baseline on it.  Both could be wrong
together, so tools/verify_extra.py checks the optimized decoder against libbz2
- an oracle we did not write - on streams the benchmark never produces: empty,
1 byte, 1 KB of zeros, runs (the RLE path), highly repetitive, English text,
64 KB of incompressible random data, and a 900 KB MULTI-BLOCK file, each at
compression levels 1 and 9.  Sixteen streams, all round-tripping exactly:

    python3 tools/verify_extra.py pyflate

It is check 2 of tools/regress.sh and runs in the verify stage of
script_pyflate.sh, so the claim is reproducible rather than asserted.  The data
is generated from a fixed seed (20260920), so a failure can be reproduced.


================================================================================
4. PERFORMANCE COMPARISON
================================================================================

END-TO-END (pyperf --rigorous, inside the guest, taskset -c 2)
---------------------------------------------------------------
    baseline    1115.1 ms +- 10 ms
    optimized    444.6 ms +-  6 ms
    ------------------------------------------------
    runtime cut 60.1%      speedup 2.51x

    pyperf compare_to -v:
      Mean +- std dev: [pyflate_baseline] 1.12 sec +- 0.01 sec
                    -> [pyflate_optimized] 445 ms +- 6 ms: 2.51x faster
      Significant (t=676.00)

    The requirement is >= 7%.  This is 60.1% - roughly 8x the threshold - and
    pyperf's own t-test rates it significant, so it is not an artefact of a
    single lucky run.

HARDWARE COUNTERS - THE "WHY" (perf stat, groups of two, 3 iterations each)
---------------------------------------------------------------------------
                          baseline          optimized       ratio
    cycles              8,298,895,683     3,302,475,210     2.51x fewer
    instructions       21,563,150,266     9,490,383,253     2.27x fewer
    IPC                          2.60              2.87     +10%
    cache-references       17,615,950         6,833,917     2.58x fewer
    cache-misses            2,779,506           525,556     5.29x fewer
      (miss rate)              15.78%             7.69%
    branch-instructions 4,343,313,029     1,752,397,613     2.48x fewer
    branch-misses          16,216,838         3,386,329     4.79x fewer
      (miss rate)               0.37%             0.19%

THIS TABLE IS THE PROOF OF THE THESIS
-------------------------------------
Two rows carry the argument.

  Cycles fell 2.51x.  Runtime fell 2.51x.  Those are the same number, which is
  the sanity check that the counters and the stopwatch are measuring the same
  thing.

  Instructions fell 2.27x while IPC moved only 2.60 -> 2.87, a 10% gain.  So
  the speedup decomposes as 2.27 x 1.10 = 2.50 - overwhelmingly "execute fewer
  instructions", with a small efficiency bonus.  The optimized code is not
  using the CPU cleverly; it is being asked to do far less.  That is exactly
  what the call-count analysis predicted, now measured at the hardware level.

The secondary effects follow from the same cause: 5.3x fewer cache misses
because ~767,000 one-byte objects are no longer allocated and chased, and 4.8x
fewer branch mispredictions because the per-candidate comparison loop in
find_next_symbol - a data-dependent branch the predictor cannot learn - is gone
entirely.

CALL-COUNT COLLAPSE
-------------------
    baseline    3,185,454 Python function calls
    optimized     582,605 Python function calls
    ------------------------------------------------
    5.47x fewer calls

PER-OPTIMIZATION ATTRIBUTION (tools/ablate.py, best of 5, in the guest)
------------------------------------------------------------------------
Ablation reverts ONE optimization at a time from the fully optimized build and
measures the slowdown.  Marginal costs do not sum to the total - the
optimizations interact - so this is a ranking, not a decomposition.

    variant                             best (ms)    marginal cost
    ---------------------------------   ---------    -------------
    baseline (all optimizations off)       1124.4
    fully optimized                         455.3
      without O3 (inverse BWT)              467.5    +12.2 ms (+3%)

    Reverting the inverse BWT costs only 3%.  Since the total saving is ~669 ms
    and O3 accounts for ~12 ms of it, O1+O2 - the canonical decoder and the
    inlined bit reader - together with O4 and O5 are responsible for
    essentially all of the speedup.

    Honest caveat: O1 and O2 cannot be ablated independently, because the
    inlined loop has no call boundary to monkey-patch.  Their combined effect
    is what is being attributed.

A DISTINCTION WORTH MAKING CAREFULLY
------------------------------------
In the OPTIMIZED build, bwt_reverse is 28.87% of runtime - the second largest
item in the profile.  Yet ablating O3 changes the total by only 3%.  These are
not in conflict, and confusing them is an easy way to misread a profile:

    share of time   how much of the remaining runtime a stage occupies
    ablation cost   how much that stage's OPTIMIZATION bought

The inverse BWT is expensive in both builds; O3 made it somewhat cheaper but
could not change its fundamental character, because a pointer chase through a
400 KB array is latency-bound and no amount of Python restructuring fixes that.
That is precisely why it is the stage the hardware accelerator targets with
on-chip SRAM - see section 5.

CORRECTNESS
-----------
    root@ubuntu:~/hwsw# python3 tools/verify.py pyflate
    pyflate
      baseline  : 399360 bytes, md5 afa004a630fe072901b1d9628b960974
      optimized : 399360 bytes, md5 afa004a630fe072901b1d9628b960974
      expected  : afa004a630fe072901b1d9628b960974
      PASS: byte-identical


================================================================================
5. HARDWARE ACCELERATION PROPOSAL
================================================================================

TARGET AND DESIGN PATTERN
-------------------------
Target: the bzip2 back end - Huffman decode, MTF, inverse BWT.

This is a DATAFLOW design, not a systolic array, and the reason is structural.
Huffman codes are variable length: the number of bits consumed per symbol is
data-dependent (1..20).  A systolic array has no control circuitry and consumes
one input per cycle unconditionally - "garbage in, garbage out" if an input is
not ready.  A variable-rate bit stream would immediately mis-align it.  Every
interface in this design therefore carries valid/ready handshakes, and the bit
window retires a data-dependent width each cycle.

(The raytrace accelerator in report_raytrace.txt is the systolic counterpart.
The two benchmarks were chosen so that one motivates each of the two dominant
accelerator paradigms from the lecture.)

BLOCK DIAGRAM
-------------

   HOST (CPU)                              BZIP2 DECODE ACCELERATOR
  +-------------------+        +---------------------------------------------+
  | Python            |        |  +-------------+                            |
  |  bz2.decompress() |        |  |  CSR block  |<--- register port ---------+--+
  |       |           |        |  |  (map below)|  (AXI4-Lite / PCIe BAR     |  |
  |       v           |        |  +------+------+   adapter: NOT built here) |  |
  | libbz2 shim       |        |         | tables, MTF alphabet, pointers    |  |
  |  (driver ioctl)   |        |         v                                   |  |
  +--------+----------+        |  +-------------+                            |  |
           |                   |  | bit_window  |  64-bit window, MSB-first, |  |
           | map buffers       |  |             |  variable-width retire     |  |
           v                   |  +------+------+                            |  |
  +-------------------+        |         | window[19:0] + valid              |  |
  | compressed buffer |        |         v                                   |  |
  | (IOVA, pinned)    |--------|--> +--------------------+                   |  |
  +-------------------+ stream |    | huffman_decoder    |                   |  |
                          in   |    | 20 comparators +   |                   |  |
  +-------------------+        |    | priority encoder   |                   |  |
  | output buffer     |<-------|--- | 1 symbol/cycle     |                   |  |
  | (IOVA, pinned)    | stream |    +---------+----------+                   |  |
  +-------------------+  out   |     sym, len |    ^ sym_ready (back-pressure)| |
           ^                   |              v    |                         |  |
  +-------------------+        |    +--------------+-----+                   |  |
  |      IOMMU        |        |    | run_expander       |                   |  |
  | per-device page   |        |    | RUNA/RUNB -> N x   |                   |  |
  | tables; the       |        |    | the MTF front byte |                   |  |
  | accelerator is a  |        |    +----+----------+----+                   |  |
  | DMA master and    |        |     idx |          ^ byte                   |  |
  | cannot escape the |        |         v          |                        |  |
  | mapped buffers    |        |    +--------------------+                   |  |
  +-------------------+        |    | mtf_unit 256 x 8b  |                   |  |
                               |    | 1-cycle shift      |                   |  |
                               |    +---------+----------+                   |  |
                               |              | block byte stream            |  |
                               |              +----------------+             |  |
                               |              v                v             |  |
                               |    +-------------------+  +------------+    |  |
                               |    | bwt_index_builder |  |   L[] SRAM |    |  |
                               |    | histogram (free), |  |  900k x 8b |    |  |
                               |    | 256-cycle prefix  |  +------------+    |  |
                               |    | sum, scatter T[]  |                    |  |
                               |    +---------+---------+                    |  |
                               |              | T[] SRAM 900k x 20b          |  |
                               |              v                              |  |
                               |    +-------------------+                    |  |
                               |    | bwt_reverse_engine|                    |  |
                               |    | 1 byte/cycle      |                    |  |
                               |    | pointer chase     |                    |  |
                               |    +---------+---------+                    |  |
                               |              | post-BWT bytes               |  |
                               |              v                              |  |
                               |    +-------------------+                    |  |
      <------- irq (done) -----+    | rle_final         |                    |  |
                               |    | 4 equal + count   |---> output stream  |  |
                               |    +-------------------+                    |  |
                               +---------------------------------------------+  |
                                                +-------------------------------+

WHAT IS BUILT HERE, AND WHAT IS NOT
-----------------------------------
Everything inside the accelerator box is RTL in hw/pyflate_decoder/ and is
exercised by simulation, including end to end (see VERIFICATION below).

Two things in the diagram are deliberately NOT built, and the report should not
pretend otherwise:

  * The AXI4-Lite (or PCIe BAR) adapter in front of the CSR port.  The CSR port
    here is a plain register interface - address, write enable, write data,
    read data.  Wrapping that in AXI4-Lite is a standard, uninteresting piece
    of glue.
  * The DMA engine that fills the input stream and drains the output stream
    from host memory.  SRC_ADDR / SRC_LEN / DST_ADDR are real registers that
    such an engine would consume, and DST_LEN is maintained by the hardware,
    but the engine itself is not part of this project.

The IOMMU is host infrastructure, not ours; it appears in the diagram because
it is what makes "the accelerator is a DMA master" safe, and because it is the
same protection boundary the bottlenecks lecture describes for NICs.

INTERFACES: INPUTS AND OUTPUTS
------------------------------
  Clock / reset          clk, rst_n.  Target 800 MHz in a 16 nm-class process;
                         the critical path is window mux -> 20 comparators ->
                         priority encoder -> subtract -> perm read.

  Compressed in          32-bit data + valid/ready + last.  Peak demand is
                         1 word / 32 cycles at 20 bits/symbol, so the input
                         interface is never the bottleneck.

  Decompressed out       8-bit data + valid/ready.  A real build packs 8 bytes
                         before the write port.  back-pressure is honoured all
                         the way back to the bit window: rle_final stalls the
                         chase, the chase stalls nothing (it is the producer),
                         and run_expander stalls the Huffman decoder.

  CSR                    Single-cycle register port; full map in
                         bzip2_accel_top.v:
                           0x00 CTRL (START/ABORT)    0x24 TBL_CTRL
                           0x04 STATUS (BUSY/DONE/    0x28 TBL_LIMIT
                                ERR/UNSUPPORTED)      0x2C TBL_BASE (commits)
                           0x08 SRC_ADDR  0x0C SRC_LEN  0x30 PERM_CTRL
                           0x10 DST_ADDR  0x14 DST_LEN  0x34 EOB_SYM
                           0x18 ORIG_PTR  0x1C BLOCK_LEN 0x38 BLK_FLAGS
                           0x20 MIN_LEN                 0x3C MTF_LOAD
                                                        0x40 DEC_LEN (RO)
                         Every register in that map is implemented and reads
                         back; DEC_LEN exists because a bzip2 block header does
                         NOT carry the block length - it is however many bytes
                         the block decodes to, so the hardware reports it.

  Interrupt              irq, asserted on DONE - which means the chase has
                         finished AND rle_final has drained, not merely that
                         the chase stopped.

  Internal widths        window 20 b (max bzip2 code length)
                         symbol  9 b (alphabet <= 258)
                         limit/base 22 b signed
                         BWT index 20 b (900 KB max block)

  On-chip memory         perm    258 x 9 b        ~0.3 KB
                         MTF     256 x 8 b        ~0.25 KB
                         hist/base 256 x 21 b     ~0.7 KB (register file)
                         T[]     900k x 20 b      ~2.25 MB  <-- dominates area
                         L[]     900k x 8 b       ~0.9 MB

INTERNAL ARCHITECTURE
---------------------
huffman_decoder.v is the heart.  Instead of iterating over code lengths, it
evaluates ALL of them at once:

    for L in 1..20 (in parallel hardware):
        code_L = window[19 -: L]                 // top L bits
        hit_L  = lvalid[L] && L >= min_len && code_L <= limit[L]
    sel = lowest L with hit_L                    // priority encoder
    sym = perm[code_sel - base[sel]]

Because Huffman codes are prefix-free, the shortest accepting length is the
unique correct one - so the priority encoder is exact and needs no backtracking.
The software's serial while-loop becomes 20 parallel 22-bit magnitude
comparators plus a 20-input priority encoder: ONE CYCLE, independent of code
length.

mtf_unit.v turns the list reorder into a single-cycle shift-enable across a
256-entry register file: every entry at index <= hit shifts down one slot
simultaneously.  Given that MTF measured 15.69% of baseline runtime - third
overall, and more than instrumentation profiling suggested - this block earns
its area.

run_expander.v is the stage that turns decoded symbols into block bytes, and
it is where bzip2's inner run-length coding lives.  Symbols 0 and 1 (RUNA and
RUNB) are not literals: they encode, in bijective base 2, how many times to
repeat the byte currently at the FRONT of the MTF list.  The expander
accumulates the count, and when the next literal arrives it emits that many
copies before letting the literal through.  Emission takes one cycle per byte,
which is what gives the Huffman decoder REAL back-pressure - sym_ready drops
while a run drains.  This is the valid/ready discipline from the accelerator
lecture doing actual work rather than being tied to 1.

rle_final.v implements the OUTER run-length coding, the last stage of the
pipeline: after four identical bytes the next byte is a count, which is
consumed rather than emitted, and that many extra copies are produced.  Its
sink can stall, and that stall propagates back through the chase.

bwt_index_builder.v builds the T[] pointer table that the chase consumes,
in three phases.  The 256-bin HISTOGRAM is folded into the MTF byte stream, so
it costs no extra pass over the block.  The PREFIX SUM is 256 cycles, fixed
regardless of block size, and overwrites the counts in place - the histogram
and the base array share one register file, which is safe because the phases
are strictly sequential.  The SCATTER then walks L[] and performs
T[base[L[i]]++] = i.  base[] is a register file rather than SRAM precisely
because the scatter needs a single-cycle read-modify-write that block RAM
cannot give.  Cost beyond decoding: 256 + 2n cycles as built (the L[] read
latency is an explicit state), or 256 + n if that read is overlapped.

bwt_reverse_engine.v keeps T[] and L[] in on-chip SRAM, with a second read port
on L[] for the builder's scatter pass.  The chain is inherently serial -
cur <- T[cur] - so the win is not parallelism but LATENCY: a guaranteed
one-cycle SRAM access replaces a dependent DRAM load that no prefetcher can
predict.  The memories are modelled the way a real SRAM behaves - the address
is presented combinationally and captured at the clock edge, the data appears
in an output register on the next cycle - and the chase is arranged so that
the output register of T[] feeds the address of the next read.  That is what
sustains ONE HOP PER CYCLE.  An earlier version registered the address as well,
which silently made the chase two cycles per hop; the testbench added for this
block caught it.  This is the stage that is still 28.87% of the OPTIMIZED
software runtime, so it is the right thing to put in hardware.

VERIFICATION - RUN INSIDE THE GUEST, NOT ASSERTED
--------------------------------------------------
  1. Golden model (hw/pyflate_decoder/golden_model.py).  Reimplements the RTL
     decision function - parallel compare + priority encode - bit for bit in
     Python, and re-decodes the real benchmark file through it:

         symbols decoded through the RTL decision function : 148271
         output bytes                                      : 399360
         md5                                               : afa004a630fe...974
         PASS

     All 148,271 symbols that occur in the workload decode correctly, so the
     parallel formulation is equivalent to the serial canonical loop for this
     workload.

  2. RTL simulation, Icarus Verilog 11.0, inside the QEMU guest.  Eight checks,
     every module covered, and the assembled design exercised end to end:

         tb_huffman_decoder    8 symbols decoded, 22 bits consumed - checks the
                               retired bit count as well as the symbol, because
                               a decoder that returns the right symbol while
                               consuming the wrong number of bits destroys the
                               rest of the stream
         tb_bit_window         12 retires, widths 1..20, across word boundaries,
                               with a retire and a refill in the same cycle
         tb_mtf_unit           returns the entry BEFORE the move and reorders
                               the list correctly; front tracks tbl[0]
         tb_run_expander       RUNA/RUNB expansion against the software loop,
                               AND that sym_ready actually drops while a run
                               drains
         tb_rle_final          outer RLE including a zero count, through a
                               stalling sink
         tb_bwt_index_builder  T[] matches the software reference exactly
         tb_bwt_reverse        the pointer chase reconstructs "banana_bandana!"
                               from its BWT, through a stalling sink
         tb_top_block          END TO END: the assembled accelerator is
                               programmed through its CSRs exactly as the
                               driver would, fed one compressed block, and
                               produces the right 15 bytes.  It also checks
                               DEC_LEN, DST_LEN, STATUS, and that a randomised
                               block and an oversized block are REFUSED
                               (Rule 3) rather than attempted

     The end-to-end vectors are generated by gen_top_vectors.py, which runs the
     bzip2 ENCODE pipeline in Python (BWT -> MTF -> RUNA/RUNB -> canonical
     Huffman), checks that the software decoder gets the original string back,
     and only then emits the testbench.  The testbench therefore cannot
     disagree with the software by construction.

     An earlier revision of this report claimed every block was "individually
     verified" when mtf_unit and bwt_reverse_engine had no testbench at all.
     That claim was false; these are the testbenches that make it true.

ACCELERATION JUSTIFICATION AND EXPECTED SPEEDUP
-----------------------------------------------
Why this component: the Huffman + MTF stages are 57.7% of baseline runtime
(42.0% + 15.7%), and the inverse BWT is still 28.9% of the OPTIMIZED runtime.
Between them they are almost the whole program, and all three are small,
well-defined, stream-shaped kernels with stable interfaces.

Estimate, stated with its assumptions:

    Symbols in the benchmark block          148,271
    Accelerator: 1 symbol/cycle @ 800 MHz   ~185 us for Huffman+MTF
    BWT chase: 399,360 cycles @ 800 MHz     ~499 us
    Total on-chip                           ~0.7 ms

    Optimized software (in the guest)       445 ms
    Baseline software (in the guest)       1115 ms

Assumptions, which matter more than the number: the block already resides in
on-chip SRAM (T[]/L[] fit), DMA setup and IOMMU mapping are excluded, and the
host is not otherwise stalled.  Real end-to-end gain would be far lower than
the raw ratio suggests, because for a 67 KB input the PCIe round trip and
driver overhead would dominate - which is exactly why an accelerator like this
belongs on-die or on a SmartNIC next to the data, not behind a discrete PCIe
card.  Stating that honestly is more useful than quoting a 600x figure.

HW/SW INTERFACE
---------------
The three rules from the accelerator-design lecture, applied concretely:

  Rule 1 - do not expect users to change their code.
      Python code still calls bz2.decompress().  Nothing above the library moves.

  Rule 2 - if software must change, confine it to the runtime/library.
      Only the decompressor library gains a fast path: map the source and
      destination buffers, push the Huffman tables through the CSR window,
      write CTRL.START, sleep on the interrupt.

  Rule 3 - do not break the user's code.
      START returns UNSUPPORTED for a randomised block or one larger than the
      on-chip index SRAM, and the driver silently falls back to software.
      Reduced benefit beats a broken program.  The optimized Python module
      mirrors this: a gzip input falls back to the baseline decoder.

Security: the accelerator is a DMA master behind the IOMMU, with per-device page
tables, so it can only touch buffers the driver mapped for this request.  This
is the same protection boundary discussed for NICs, and the reason the driver
maps per-request instead of pinning memory permanently.

PERFORMANCE / AREA / POWER TRADE-OFFS
-------------------------------------
  Area is dominated by memory, not logic.  T[] + L[] for a 900 KB block is
  ~3.15 MB of SRAM; the decoder logic (20 comparators, a priority encoder, a
  256-entry register file) is negligible beside it.

  The obvious trade: shrink the on-chip block memory and spill T[] to DRAM.
  That reintroduces exactly the dependent-load latency the accelerator existed
  to remove, so the inverse BWT would collapse back toward software speed while
  Huffman decode stayed fast.  Supporting only the smaller bzip2 block sizes
  (-1 .. -4, i.e. 100-400 KB) is the better compromise, with larger blocks
  reported as UNSUPPORTED and handled in software under Rule 3.

  Frequency: the critical path is the comparator-plus-priority-encoder chain.
  Splitting it into two pipeline stages would push past 1 GHz at the cost of a
  one-cycle bubble on table switches (every 50 symbols in bzip2), which is
  nearly free - a good trade if timing closure demands it.

  Power: the MTF register file's parallel 256-entry shift is the largest
  per-symbol switching cost.  Gating the shift for index 0 (a no-op, and common
  because MTF concentrates hits at the front) removes a large fraction of that
  toggling for a trivial comparator.


================================================================================
6. CONCLUSION
================================================================================

The optimized decoder is 2.51x faster than the baseline inside the course QEMU
guest while producing byte-identical output, verified by MD5 over all 399,360
decompressed bytes.

The lesson is not "use a better Huffman algorithm".  The profile showed the
linear table scan almost always exits after about two probes, so the scan was
never really the problem - the problem was that decoding one symbol cost
roughly seven Python function calls.  Removing calls, not iterations, produced
the 5.5x reduction in call count and with it the speedup.  The hardware
counters settle the argument: cycles and runtime both fell 2.51x, instructions
fell 2.27x, and IPC moved only 2.60 -> 2.87.  The machine did not get smarter;
it was simply asked to do far less.

Two methodology points proved their worth.  First, py-spy and cProfile
disagreed about move_to_front - 15.69% versus 10.1% - and the sampler was
right, because instrumentation charges per call while sampling charges per
cycle.  Second, share-of-time and ablation-cost are different questions: the
inverse BWT is 28.87% of the optimized runtime yet its optimization was worth
only 3%, because a latency-bound pointer chase cannot be fixed by restructuring
Python.

Building the measurement inside the VM also changed what was measurable, and
that is worth recording: hardware counting works only because the guest is
launched with -cpu host,pmu=on; hardware SAMPLING does not work at all, so
flame graphs use cpu-clock; six events at once silently starve one to zero; and
CPython 3.10 cannot give perf Python symbols, which is why py-spy carries the
Python-level analysis.  None of that was obvious in advance, and each was found
by testing rather than assumed.

In hardware the same kernel maps naturally onto a dataflow design: the serial
canonical decode collapses into 20 parallel comparators and a priority encoder,
one symbol per cycle, verified both by a golden model across all 148,271
symbols and by RTL simulation under Icarus Verilog in the guest.  The
variable-length nature of the codes is precisely what rules out a systolic
array here and makes this the dataflow half of the two-paradigm story completed
by the raytrace accelerator.

The honest limit of the hardware proposal is the memory, not the logic: the
inverse BWT needs megabytes of on-chip SRAM to keep its promise, and without
that it degenerates to the same un-prefetchable DRAM pointer chase that makes
it slow in software.


================================================================================
7. CRITICAL REVIEW - KNOWN LIMITATIONS AND WHAT TESTING FOUND
================================================================================

This section exists because "it passed" is not the same as "it is correct".
Everything below was found by deliberately attacking the implementation after
it was written, and is stated rather than quietly fixed or hidden.

BUGS FOUND AND FIXED
--------------------
  fp32_add normalize (raytrace)  -  FOUND BY SIMULATION, FIXED
      Failed on its second test vector: 2+3 gave 6.5, 9-4 gave 2.5.  The
      carry-out path normalized the leading one to bit 26 while the
      leading-zero path normalized to bit 27, and the mantissa slice matched
      neither.  Fixed to one bit-26 convention, mant_raw = sum_n[25:3].

  Fake valid signalling (raytrace)  -  FOUND BY REVIEW, FIXED
      fp32_mul and fp32_add tied out_valid to 1 and discarded in_valid, so
      every downstream handshake was meaningless.  Valid now propagates one
      register per pipeline stage.

  Four array-level alignment bugs (raytrace)  -  FOUND BY REVIEW, FIXED
      ray_sphere_array computed t as v_arr[0] - sqrt(disc) for every candidate,
      carried no identity across the 25-cycle sqrt, advanced its index counter
      only on hits, and never re-initialized the running minimum between rays.
      None were visible from the PE testbench.  Fixed with an aligned delay
      line and a per-ray accumulator; tb_ray_array.v now checks that the
      NEAREST of 8 spheres wins and is named correctly.

  Capture bank missing (raytrace)  -  FOUND BY REVIEW, FIXED
      All N PEs produce their results on the same cycle, but the serializer
      read them live over N cycles - sampling N different rays instead of the
      N spheres of one ray.  It only appeared to work because out_valid was
      stuck high.  Results are now latched into a capture bank and a ray_ready
      handshake tells the host the array accepts one ray every N cycles.

  L[] write address tied to zero (pyflate)  -  FOUND BY REVIEW, FIXED
      bzip2_accel_top wrote every decoded byte to L[0].  A write-address
      counter now advances with the MTF output stream.

  bwt_index_builder read timing  -  FOUND BY SIMULATION, FIXED
      The scatter pass registered l_raddr, so L[] data arrived one cycle after
      it was used and every T[] entry was written with the wrong index -
      T = [2,4,x,0,3,5] instead of [1,3,5,0,2,4].  l_raddr is combinational
      now, so the memory latches L[scat_i] on the edge that ends the wait state
      and the data is valid when the scatter consumes it.

  `bins` is a SystemVerilog keyword  -  FOUND BY COMPILING, FIXED
      The histogram array was originally called `bins`, which is reserved in
      SystemVerilog (covergroup syntax).  Under -g2012 - which is what the run
      scripts use - every reference to it became a syntax error, reported as
      "invalid module item" at the declaration.  Renamed to `hist`.

  Broken line continuation in script_pyflate.sh  -  FOUND BY INSPECTION, FIXED
      The iverilog elaboration command ended a line with two backslashes, which
      bash reads as an escaped literal backslash rather than a continuation, so
      the command silently split in two.  `bash -n` accepts both forms, which is
      why it needed an explicit check (tools/check for even-numbered trailing
      backslashes).

  ---- found by a component-by-component audit against the course material,
       after the software side was already finished ----

  Both run-length stages were MISSING (pyflate)  -  FOUND BY AUDIT, FIXED
      bzip2 has two different run-length codings and the accelerator
      implemented neither.  The top level dropped RUNA/RUNB symbols outright
      (`mtf_valid = sym_valid && !is_run`), so every run in the block vanished,
      and there was no outer RLE stage at all even though the block diagram in
      section 5 drew one.  run_expander.v and rle_final.v now implement both,
      each with its own testbench.  This was the largest gap in the project:
      the datapath could not have produced correct bytes for real data.

  The MTF alphabet was never loaded (pyflate)  -  FOUND BY AUDIT, FIXED
      mtf_unit has a load port; the top level tied load_en to 0, so the 256
      entries were undefined and every byte the unit emitted was meaningless.
      CSR 0x3C now loads the alphabet, exactly as the driver would after
      reading the block's `used` bitmap.

  Two dependent memory reads in one cycle (pyflate)  -  FOUND BY AUDIT, FIXED
      bwt_reverse_engine computed `cur <= T[cur]` and `out_data <= L[T[cur]]`
      in the same cycle.  That is two dependent reads in one cycle, which only
      works with asynchronous-read memory - i.e. flip-flops, not the SRAM the
      area estimate assumes.  The engine now models a real synchronous SRAM
      (address captured at the edge, data in an output register) and pipelines
      the chase so it still retires one byte per cycle.

  ... and the first attempt at that fix was wrong  -  FOUND BY SIMULATION
      Registering the address AND the data made the chase two cycles per hop
      while still producing plausible-looking output.  tb_bwt_reverse.v - the
      testbench written in the same pass - caught it on the first run.  This is
      the second time in this project that a "careful" hardware fix was wrong
      until simulated.

  Registers that existed only in a comment (pyflate)  -  FOUND BY AUDIT, FIXED
      The CSR map documented SRC_ADDR, SRC_LEN, DST_ADDR, DST_LEN, CTRL.ABORT
      and STATUS.ERR/UNSUPPORTED; the register file implemented none of them.
      All are real now, read back, and UNSUPPORTED is actually asserted for a
      randomised or oversized block - which is what makes the Rule 3 claim
      testable.  tb_top_block checks both refusal paths.

  Back-pressure asserted but not implemented (pyflate)  -  FOUND BY AUDIT, FIXED
      sym_ready was tied to 1, so the valid/ready discipline the report
      describes was half a handshake.  run_expander now drops sym_ready while a
      run drains, and the testbench checks that it does.

  A false claim in this report  -  FOUND BY AUDIT, CORRECTED
      Section 7 previously stated that every block was "individually verified",
      listing mtf_unit and bwt_reverse_engine.  Neither had a testbench.  Both
      do now (tb_mtf_unit.v, tb_bwt_reverse.v), and the statement is true.

KNOWN LIMITATIONS - STATED, NOT FIXED
-------------------------------------
  1. The end-to-end simulation runs a SMALL block, not the benchmark file.
     tb_top_block.v programs the accelerator through its CSRs, streams one
     compressed block in and checks the bytes that come out, so the assembled
     design does now decompress - but the block is 15 bytes, generated by
     running the bzip2 encoder in Python.  Feeding interpreter.tar.bz2 through
     the RTL (148,271 symbols, 399,360 output bytes) would take a full driver
     model and a long simulation; it has not been done.

  2. ONE Huffman table at a time.  A real bzip2 block carries up to six tables
     and switches between them every 50 symbols according to a selector list.
     The hardware holds one table set, so the driver would have to reprogram it
     at every switch - roughly 3,000 reprogram bursts for the benchmark block,
     which would dominate the CSR traffic.  A production design would hold all
     six banks and select with the decoded selector stream.  The end-to-end
     test uses a single-table block, so it does not exercise this at all.  This
     is the biggest remaining gap between the model and a usable accelerator.

  3. The block header is parsed in software, by design.  The accelerator is
     programmed with tables, alphabet, origin pointer and end-of-block symbol;
     it never sees the bzip2 stream header.  That follows Rule 2 - keep the
     changes in the library - but it does mean the driver does real work per
     block, which the speedup estimate below does not charge for.

  4. No AXI4-Lite adapter and no DMA engine.  The CSR port is a plain register
     interface and the data ports are streams; the adapter and the engine that
     would move bytes to and from host memory are standard glue and are not
     built.  Section 5 marks them on the block diagram as external.

  5. build_decode_tables() does not skip zero-length symbols, while the
     baseline's HuffmanTable does (`if bits:`).  Instrumenting the real
     workload shows all 6 Huffman tables have minimum length 2, so no
     zero-length code ever occurs and the two agree.  It is a latent
     divergence on input this workload never produces, not an active bug.

  6. The optimized software keeps one inherited quirk on purpose: the selector
     loop tests `selector_pointer <= len(selectors_list)`, one past the end.
     The baseline does exactly the same (pyflate_baseline.py, line 421) and the
     end-of-block symbol always breaks the loop first, so neither version can
     reach it.  It is copied rather than corrected because the mandate is
     byte-identical output, not a better decoder.

  7. fp32_sqrt truncates, so the raytrace accelerator is NOT bit-identical to
     math.sqrt() - 1.16 ulp worst case over 6,008 samples.  The pyflate
     accelerator IS bit-exact.

  8. Subnormal inputs and outputs are flushed to zero in the FP units.  This is
     a deliberate graphics-datapath choice, and it means a scene pushing into
     the subnormal range would diverge from the software reference.

  9. The raytrace array BROADCASTS each ray to all 8 PEs rather than pulsing it
     from neighbour to neighbour.  It is weight-stationary with control-free
     PEs - the two properties that matter at this scale - but it is not a
     textbook systolic array, and the report says so rather than borrowing the
     word.  ray_sphere_array.v quantifies the trade.

 10. No CPI stack in the guest: stalled-cycles-frontend and
     stalled-cycles-backend are <not supported>, so the hierarchical stall
     breakdown from lecture 4 cannot be reproduced inside the VM.

VERIFICATION STATUS
-------------------
  tools/regress.sh runs all fifteen checks.  All fifteen pass inside the QEMU
  guest under Icarus Verilog 11.0:

    SOFTWARE   correctness gate (both benchmarks, byte/bit-identical)
               extended correctness (16 bzip2 streams vs libbz2, an
                 independent oracle; raytrace at 7 resolutions)
               huffman golden model (all 148,271 real symbols)
               fp32_sqrt golden model (6,008 samples)
    pyflate    tb_huffman_decoder      symbol AND retired bit count
               tb_bit_window           12 irregular retires, 1..20 bits
               tb_mtf_unit             pre-move entry, list reorder, front
               tb_run_expander         RUNA/RUNB vs software, back-pressure
               tb_rle_final            outer RLE incl. zero count, stalling sink
               tb_bwt_index_builder    T[] matches the software reference
               tb_bwt_reverse          pointer chase vs software, stalling sink
               tb_top_block            END TO END: whole block through the
                                       assembled design, plus both Rule 3
                                       refusal paths
    raytrace   tb_ray_sphere           PE, bit-exact worked case
               tb_ray_array            nearest-hit across 8 spheres
               tb_fp_random            400 randomized vectors, bit-exact

  Every RTL module in hw/pyflate_decoder/ now has a testbench, and the
  assembled top level is exercised end to end rather than merely elaborated.

A NOTE ON WHERE THIS WAS MEASURED
---------------------------------
An earlier revision of this report carried numbers measured on the naranja7
HOST rather than inside the guest.  That violated the course rule that all work
happens in the provided QEMU image, and the report even argued the host was
preferable because CPython 3.12 supports -X perf and the host PMU is
unrestricted.  That was a technical argument placed above a course requirement.
Everything was re-measured inside the guest and every figure in this report
replaced.  The host numbers are retained only in results_naranja7/ as a
labelled cross-check, because the speedup ratios turning out to be
platform-independent (2.38x host vs 2.51x guest for pyflate) is itself worth
knowing - but they are not the submitted result.
